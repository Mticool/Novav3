# TAPNOW CLONE - PROJECT CONTEXT

## ЧТО МЫ СТРОИМ

Копия TapNow (https://app.tapnow.ai) — визуальный редактор нодов для AI-генерации контента (изображения, видео, текст).

### Референс
- Оригинал: https://app.tapnow.ai/#/canvas/6d0228a0-bdb6-43f6-a0fe-8ef73649c842
- Примеры нодов: Text → Image → Video → Upscale

## АРХИТЕКТУРА

### СТЕК
- React 18 + TypeScript
- Vite
- @xyflow/react (для canvas с нодами)
- Zustand (state management)
- Tailwind CSS
- lucide-react (иконки)

### СТРУКТУРА UI

```
┌─────────────────────────────────────────────────────┐
│ TopBar: [Logo] [Project Name] ... [Credits] [Share]│
├──┬──────────────────────────────────────────────────┤
│  │                                                   │
│S │              CANVAS (React Flow)                  │
│i │                                                   │
│d │   ┌──────┐      ┌──────┐      ┌──────┐          │
│e │   │Text  │─────>│Image │─────>│Video │          │
│b │   │Node  │      │Node  │      │Node  │          │
│a │   └──────┘      └──────┘      └──────┘          │
│r │                                                   │
│  │                                                   │
└──┴──────────────────────────────────────────────────┘
```

#### Левый Sidebar (56px)
- Вертикальный список иконок
- Кнопка + (Add Nodes) сверху
- Library, Workflows, Comments, History
- User avatar снизу

#### Canvas
- React Flow с бесконечным полотном
- Зум (scroll), панорамирование (drag)
- Ноды перетаскиваются
- Связи через handles (точки слева/справа)

#### Top Bar
- Editable project name input
- Credits counter (285)
- Share button
- Community dropdown

## НОДЫ

### Типы нод
1. **Text Node** (зелёная)
   - Textarea для промпта
   - Character counter
   
2. **Image Node** (синяя)
   - Upload area (drop/click)
   - Preview изображения
   - Download button
   
3. **Video Node** (фиолетовая)
   - Video preview или placeholder
   - Progress indicator

### Структура ноды
```
┌─────────────────────┐
│ ← [Title Input]   → │  ← Handles слева/справа
├─────────────────────┤
│                     │
│   Content Area      │
│                     │
├─────────────────────┤
│ [Actions: Download] │
└─────────────────────┘
```

### States нод
- **idle**: пустая нода, waiting for input
- **loading**: спиннер, progress bar
- **success**: зелёная галочка, результат
- **error**: красная обводка, error message

## ADD NODES МЕНЮ

Выезжает слева при клике на +

```
┌─────────────────────┐
│ Add Nodes       [×] │
├─────────────────────┤
│ 📝 Text   [Coming]  │
│ 🖼  Image  [Pro]     │
│ 🎥 Video            │
│ 🎵 Audio   [Beta]   │
├─────────────────────┤
│ Add Source          │
│ 📤 Upload           │
└─────────────────────┘
```

Ширина: 220px
Item height: 48px

## ДИЗАЙН СИСТЕМА

### Цвета
```css
--canvas-bg: #0a0a0a
--node-bg: rgba(26, 26, 26, 0.95)
--node-border: rgba(255, 255, 255, 0.05)
--text-primary: #ffffff
--text-secondary: #cccccc
--text-muted: #888888
--accent-blue: #4a9eff
--edge-color: rgba(255, 255, 255, 0.15)
```

### Размеры
- Node min-width: 120px
- Node max-width: 420px
- Node border-radius: 16px
- Node padding: 12px
- Handle size: 12px
- Edge width: 2px
- Sidebar width: 56px
- TopBar height: 56px

### Шрифты
- Family: Inter
- Base: 13px
- Node title: 14px, font-weight 600
- Small text: 11px

### Анимации
- Hover node: scale(1.02), усиление тени
- Handle hover: bg #4a9eff, scale(1.25)
- Menu slide-in: 250ms cubic-bezier

## ЛОГИКА

### Zustand Store
```ts
interface Store {
  nodes: Node[]
  edges: Edge[]
  projectName: string
  credits: number
  
  // Actions
  addNode: (type: NodeType) => void
  updateNode: (id: string, data: Partial<NodeData>) => void
  deleteNode: (id: string) => void
  connectNodes: (source: string, target: string) => void
}
```

### Связи между нодами
- Source handle (справа) → Target handle (слева)
- Bezier curves, сглаженные
- Валидация: text → image ✅, image → image ❌

### Keyboard Shortcuts
- Cmd/Ctrl+S: Save
- Delete: удалить выбранную ноду
- Cmd/Ctrl+Z: Undo
- Space+Drag: Pan canvas

## MVP SCOPE

### Phase 1 (текущая)
✅ Canvas с React Flow
✅ 3 типа нод (Image, Text, Video)
✅ Sidebar с Add Nodes
✅ Top Bar
✅ Handles и связи
✅ Базовый дизайн

### Phase 2 (следующая)
- Upload изображений
- Контекстные тулбары
- Keyboard shortcuts
- History (undo/redo)

### Phase 3 (будущее)
- AI Chat panel
- Workflows templates
- Real-time collaboration

## ПРАВИЛА РАЗРАБОТКИ

1. **Используй TypeScript везде**
2. **Все компоненты memo() для оптимизации**
3. **Tailwind классы вместо inline styles**
4. **Zustand для глобального state**
5. **React Flow handles для связей**
6. **Темная тема по умолчанию**
7. **Анимации через CSS transitions**
8. **Никаких внешних UI библиотек (кроме lucide-react для иконок)**

## ФАЙЛОВАЯ СТРУКТУРА

```
src/
├── App.tsx                 # Root component
├── main.tsx               # Entry point
├── index.css              # Global styles
│
├── components/
│   ├── Canvas.tsx         # React Flow wrapper
│   ├── TopBar.tsx         # Header
│   ├── Sidebar.tsx        # Left sidebar
│   ├── AddNodesMenu.tsx   # Add nodes dropdown
│   │
│   └── nodes/
│       ├── ImageNode.tsx  # Image node component
│       ├── TextNode.tsx   # Text node component
│       └── VideoNode.tsx  # Video node component
│
├── store/
│   └── useStore.ts        # Zustand store
│
└── types/
    └── index.ts           # TypeScript types
```

## ВАЖНО ДЛЯ AI АССИСТЕНТА

- Всегда проверяй референс TapNow перед генерацией кода
- Используй точные цвета и размеры из дизайн-системы
- React Flow — это обязательно, никаких кастомных решений
- Handles должны быть видимы и кликабельны
- Линии связей должны быть плавными Bezier кривыми
- Все ноды должны иметь одинаковую базовую структуру

