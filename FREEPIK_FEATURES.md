# 🎨 Freepik Spaces Features - Implementation Guide

Полная реализация node-based canvas системы в стиле Freepik Spaces.

## ✨ Новые функции

### 1. 🔧 Интерактивные ноды с параметрами

#### **Enhancement Node** (Улучшение изображения)
- **Функция**: Улучшение качества изображений через настройку параметров
- **Параметры**:
  - Резкость (Sharpness): 0-100
  - Контраст (Contrast): 0-100
- **Входы**: Изображение (синий handle)
- **Выходы**: Улучшенное изображение (зеленый handle)

**Как использовать**:
1. Добавьте Enhancement Node через меню "+" → "Инструменты"
2. Подключите Image Node или ImageUpload к входу
3. Выберите ноду и настройте слайдеры Резкость и Контраст
4. Нажмите Play для применения изменений

#### **Camera Angle Node** (Угол камеры)
- **Функция**: Изменение ракурса объектов на изображении
- **Параметры**:
  - Повернуть (Rotate): 0-360°
  - Вертикальный (Vertical): -30° до 90°
  - Масштаб (Zoom): 0.5 до 3.0
- **Входы**: Изображение
- **Выходы**: Трансформированное изображение

**Как использовать**:
1. Добавьте Camera Angle Node через меню
2. Подключите изображение с объектом/персонажем
3. Настройте параметры через интерактивные слайдеры
4. Кнопка Reset сбрасывает все значения к дефолтным

---

### 2. 🎯 Toolbar (Левая панель инструментов)

Профессиональная панель инструментов в стиле Freepik Spaces:

#### Инструменты:
- **Selection** (Указатель) - выбор и перемещение нодов
- **Pan** (Рука) - панорамирование canvas
- **Scissors** (Ножницы) - обрезка связей
- **Comment** (Комментарий) - добавление заметок

#### История:
- **Undo** (⏪) - отмена действия (Ctrl+Z / ⌘+Z)
- **Redo** (⏩) - повтор действия (Ctrl+Shift+Z / ⌘+Shift+Z)
- Максимум 50 шагов в истории
- Автоматическое сохранение перед каждым действием

#### Настройки:
- **Settings** - доступ к глобальным настройкам

---

### 3. 📜 History System (Undo/Redo)

Полнофункциональная система истории изменений:

**Возможности**:
- ✅ Отмена до 50 последних действий
- ✅ Повтор отмененных действий
- ✅ Умное сохранение (пропускает дубликаты)
- ✅ Сохранение состояния нодов и связей

**Горячие клавиши**:
- `Ctrl+Z` / `⌘+Z` - Undo
- `Ctrl+Shift+Z` / `⌘+Shift+Z` - Redo
- `Ctrl+Y` / `⌘+Y` - Redo (альтернатива)

**Что сохраняется**:
- Позиции нодов
- Параметры нодов
- Связи между нодами
- Выделение

---

### 4. 🔗 Валидация типов при подключении

Умная система проверки совместимости при соединении нодов:

#### Типы данных:
- **Text** (текст) - промпты, описания
- **Image** (изображение) - картинки, фотографии
- **Video** (видео) - видеоконтент
- **Any** (любой) - универсальный тип

#### Правила подключения:
- ✅ Text → Image (генерация из текста)
- ✅ Text → Video (text-to-video)
- ✅ Image → Video (image-to-video)
- ✅ Image → Enhancement (улучшение)
- ✅ Image → Camera Angle (трансформация)
- ❌ Video → Image (невозможно)
- ❌ Image → Text (невозможно)

#### Защита от циклов:
- Автоматическое обнаружение циклических зависимостей
- Предотвращение бесконечных циклов
- Понятные сообщения об ошибках

**Пример ошибки**:
```
❌ Невозможно подключить: Incompatible types: video/image → text
```

---

### 5. 🎨 Freepik-Style визуальные улучшения

#### Handles (точки подключения):
- **Синие (#0088ff)** - входы (inputs)
- **Зеленые (#22dd88)** - выходы (outputs)
- Увеличение при hover: scale(1.4)
- Glow эффект при наведении

#### Edges (связи):
- **Базовый цвет**: зеленый (#22dd88)
- **Толщина**: 2px (3px при hover)
- **Hover**: усиленное свечение
- **Selected**: желтый неон (#EFFE17)

#### Анимации:
- **Pulse effect** для активных соединений
- **Flow dots** для передачи данных
- **Smooth transitions** (0.25s ease)

---

### 6. 🎹 Горячие клавиши

#### Управление:
- `Delete` / `Backspace` - удалить выбранные ноды
- `Ctrl+A` / `⌘+A` - выбрать все
- `Ctrl+S` / `⌘+S` - сохранить workflow (скачать JSON)

#### История:
- `Ctrl+Z` / `⌘+Z` - отменить
- `Ctrl+Shift+Z` / `⌘+Shift+Z` - повторить
- `Ctrl+Y` / `⌘+Y` - повторить (Windows)

#### Canvas:
- `Space + Drag` - панорамирование
- `Scroll` - масштабирование
- `Shift + Click` - множественный выбор

---

## 🚀 Quick Start

### Создание простого workflow:

1. **Добавьте Text Node**
   ```
   Клик на "+" → Узлы → Текст
   Введите промпт: "Cyberpunk city at night, neon lights"
   ```

2. **Добавьте Image Node**
   ```
   Клик на "+" → Узлы → Генератор изображений
   ```

3. **Соедините ноды**
   ```
   Перетащите от зеленого handle Text к синему handle Image
   ```

4. **Добавьте Enhancement**
   ```
   Клик на "+" → Инструменты → Улучшение изображения
   Соедините Image → Enhancement
   Настройте Резкость: 80, Контраст: 60
   ```

5. **Добавьте Camera Angle**
   ```
   Клик на "+" → Инструменты → Угол камеры
   Соедините Enhancement → Camera Angle
   Поворот: 45°, Вертикальный: 10°, Масштаб: 1.2
   ```

---

## 📐 Технические детали

### Архитектура валидации:
```typescript
// Определение типов нодов
NODE_TYPE_DEFINITIONS = {
  image: {
    inputs: ['text'],
    outputs: ['image']
  },
  enhancement: {
    inputs: ['image'],
    outputs: ['image']
  }
}

// Проверка совместимости
isValidConnection(sourceType, targetType) → { valid, reason }
```

### History механизм:
```typescript
interface HistorySnapshot {
  nodes: Node[];
  edges: Edge[];
}

// Сохранение состояния
saveToHistory() → past: [snapshot1, snapshot2, ...]

// Отмена
undo() → восстанавливает past[last], сохраняет в future

// Повтор
redo() → восстанавливает future[0], сохраняет в past
```

---

## 🎯 Best Practices

1. **Используйте валидацию**
   - Система автоматически проверяет типы
   - Следуйте логическому порядку: Text → Image → Video

2. **Используйте историю**
   - Ctrl+Z/Ctrl+Y для быстрой итерации
   - Не бойтесь экспериментировать

3. **Настраивайте параметры**
   - Слайдеры реагируют в реальном времени
   - Сохраняются автоматически

4. **Сохраняйте workflow**
   - Ctrl+S для экспорта в JSON
   - Импорт через Templates

---

## 🐛 Troubleshooting

### Проблема: "Невозможно подключить"
**Решение**: Проверьте типы данных. Используйте промежуточные ноды.

### Проблема: Undo не работает
**Решение**: Убедитесь, что есть сохраненная история (проверьте console.log)

### Проблема: Слайдеры не обновляются
**Решение**: Выберите ноду (клик), чтобы показать параметры

---

## 📚 API Reference

### Store Methods
```typescript
// History
undo() → void
redo() → void
canUndo() → boolean
canRedo() → boolean
saveToHistory() → void

// Nodes
addNodeAt(type, position) → string
updateNode(id, data) → void
deleteNode(id) → void
```

### Validation
```typescript
isValidConnection(source, target) → { valid, reason }
wouldCreateCycle(edges, newSource, newTarget) → boolean
getConnectionErrorMessage(source, target) → string
```

---

## 📝 Changelog

### v3.0.0 - Freepik Features
- ✅ Enhanced Image Node с интерактивными слайдерами
- ✅ Camera Angle Node с 3D трансформациями
- ✅ Professional Toolbar с инструментами
- ✅ Complete History System (50 шагов)
- ✅ Type Validation для подключений
- ✅ Freepik-style визуальные улучшения
- ✅ Keyboard Shortcuts (Undo/Redo/Save)
- ✅ Cycle Detection для защиты от ошибок

---

**🎉 Ready to create professional AI workflows!**
